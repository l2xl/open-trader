accept: |
  after review the item YAML shows the stamp and per-reference fingerprints and the gate is green; editing a referenced test afterwards fails the gate naming item and file; one commit carrying both an edit and a re-stamp fails the self-approval check while the same change split into two commits passes.
active: true
derived: false
header: |
  Review stamping on approval
level: 1.4.0
links:
- INFRA-041: bJ_QCcmVEwlqU5DAp9eZmKhuXUiSannw2A7pkvD1y8Q=
normative: true
ref: ''
references:
- keyword: INFRA-049
  path: scripts/tests/test_infra_049_freeze_workflow.py
  sha: bc7bcd25a2e7c199b26dc77285f832e09a79d6c5cf76a13b05aa0213e75f1311
  type: file
reviewed: EEVGTaKJoFjb3Qvzx0S_mYLPGZ0WDmAQskIcBhjFgC0=
test: |
  single pytest file: review round-trip stamping in a tmp fixture tree, frozen-test change detection, self-approval rejection on a fixture git history (including acceptance of a no-ff merge of a compliant two-commit history), and a workflow-lint assertion that CI runs the self-approval check unconditionally on every push and pull request.
text: |
  On explicit user approval a requirement shall be frozen, recording fingerprints of the item and its bound test files; the gate shall fail when a frozen item's bound test changes after review, and a single commit may not both modify a frozen item or its bound test and re-approve it
verify: test
