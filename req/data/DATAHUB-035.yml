header: Late subscriber snapshot
description: |
  A subscriber attaching to a non-empty feed shall immediately receive a snapshot of the current cache without waiting for the next accept.
parents: [DATAHUB-030]
order: 50
tests: ~
