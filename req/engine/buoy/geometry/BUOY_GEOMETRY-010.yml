header: Degenerate single-price period
description: |
  Constructing a geometry candle whose high does not exceed its low shall be
  rejected with an invalid-argument error whatever its traded volume; the
  renderer substitutes its fixed-size marker for such periods instead of a
  sampled contour.
parents: [BUOY_GEOMETRY]
order: 100
tests: ~
