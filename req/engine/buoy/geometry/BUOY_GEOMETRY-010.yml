header: Degenerate single-price period
description: |
  A period whose high equals its low shall be reported as degenerate whatever its
  traded volume, so the renderer substitutes its fixed-size marker instead of a
  sampled contour.
parents: [BUOY_GEOMETRY]
order: 100
tests: ~
