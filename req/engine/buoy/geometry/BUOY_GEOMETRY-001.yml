header: Window calibration medians
description: |
  Calibration over a range of periods shall yield the median traded volume and
  the median summed per-side deviation, both taken in a single pass over the
  range's periods, excluding empty periods and single-price degenerate periods.
parents: [BUOY_GEOMETRY]
order: 10
tests: ~
