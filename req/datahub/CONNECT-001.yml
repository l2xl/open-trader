header: Connect component
description: |
  The connect transport layer: shared TLS connection context, asynchronous HTTPS request (http_query) and websocket streaming (websock_connection) with typed error propagation (src/connect/).
parents: [DATA]
order: 20
